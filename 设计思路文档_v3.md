# 全栈开发 Agent 团队 —— 设计思路文档（v3.0）

> **本版（v3.0）相对 v2.0 的主要变化：**
> 1. **前端从"原生 JS"改为 [Petite Vue](https://github.com/vuejs/petite-vue)（CDN / IIFE，零构建步骤）**：让框架接管数据绑定与自动重渲染，LLM 不再手写易错的原生状态管理与事件委托。
> 2. **新增第 11 项 QA 检查（前端框架健全性）**：运行时冒烟只启动后端、不执行前端 `app.js`，所以"框架用错、页面是死的"会漏网；新增静态检查专门守住这一类回归。
> 3. **人工介入（HITL）从"满意 / 中止"两态升级为完整的"审核 → 重生成"闭环**：PRD 支持直接编辑或带反馈重生成、架构支持带反馈重生成、QA 新增人工裁决（强制通过 / 继续修复 / 中止）；新增 `human_review` 模块集中承载这套逻辑。
> 4. **黑板新增 `user_feedback` 字段**：`summary_for()` 在重生成轮次自动把用户反馈注入对应 Agent 的 prompt。
> 5. **浏览器 UI（`api.py` + `web_ui.html`）**：工程清单中已包含这两个文件，但本次未能读取其内容，故本文档暂未对其展开（见 §7）。
>
> 文末「实现思路历程与踩坑记录」与「Future Work」均按本版更新。

---

## 一、项目概述

本项目实现了一个"全栈开发 Agent 团队"系统：用户只需输入一句话需求（如"帮我做一个待办事项 Web 应用"），系统通过多个 AI Agent 协作，自动产出一个**完整、可运行、零原生依赖**的 Web 项目，包括前端代码、后端代码、本地数据存储、README 及启动脚本。

核心设计理念有三条：

1. **不是一个超大单 Agent 一把梭，而是多个职责分明的 Agent 组成团队，按流水线协作。**
2. **不只生成，还要验证并自我修复**：QA Agent 用确定性检查 + 真实运行时冒烟测试守底线，发现问题后流水线会自动重跑相关 Agent，最多 3 轮，形成闭环。
3. **人始终在关键决策点保有控制权**：在 PRD、架构、QA 三个节点支持人工介入，可批准、编辑、反馈重生成或中止，避免"机器全绿但人不满意"的产物被一路放行（v3.0 强化，见 4.5）。

---

## 二、系统架构总览

### 2.1 Agent 团队成员与职责边界

系统由 6 个 Agent 组成，每个 Agent 有清晰的职责边界，只做自己该做的事：

| Agent | 角色 | 职责 | 产出物 |
|-------|------|------|--------|
| **ProductManager** | 产品经理 | 把模糊需求转化为结构化 PRD | PRD（JSON） |
| **Architect** | 架构师 | 基于 PRD 设计技术方案和 API 契约 | 技术架构 + API Contract + 数据模型 |
| **Backend** | 后端工程师 | 按 API 契约实现后端代码 | `server.js`、`db.js`、`package.json` |
| **Frontend** | 前端工程师 | 按 API 契约实现 **Petite Vue 单页应用** | `public/index.html`、`public/app.js`（`styles.css` 由系统注入） |
| **DevOps** | 运维工程师 | 生成工程化交付文件 | `README.md`、`start.sh`、`.gitignore` |
| **QA** | 测试工程师 | 静态检查 + 契约一致性 + **运行时冒烟** + **前端框架健全性** | QA 质检报告（11 项） |

> v3.0 变化：Frontend 的实现技术从"原生 HTML/JS"换成 Petite Vue（见 2.3 与 5.11）；QA 的检查项从 10 项增至 11 项（见 4.1）。

### 2.2 流水线执行顺序

```
用户需求 → PM → Architect → Backend → Frontend → DevOps → QA → 可运行项目
```

这是一条**严格的串行流水线**，每个 Agent 依赖上游 Agent 的产出。

流水线末端带一个**修复回边**：QA 不通过时，编排器会把失败项映射到具体 Agent 并重跑，直到全绿或达到轮次上限。

在开启人工介入（HITL）时，PM、Architect、QA 三个节点还各自带一个**审核 → 重生成**的小循环（见 4.5）：

```
            ┌─审核─┐                 ┌─审核─┐                          ┌─审核─┐
 需求 → PM ─┤      ├→ Architect ─────┤      ├→ Backend→Frontend→DevOps→ QA ─┤      ├→ 项目
            └─重生成┘(≤MAX_REGEN)     └─重生成┘(≤MAX_REGEN)                └─修复─┘(≤MAX_QA_ROUNDS)
```

- PM 的 PRD 是所有后续 Agent 的需求基础。
- Architect 的 API Contract 是前后端的"合同"，确保两端不打架。
- Backend 和 Frontend 严格按照 Contract 实现，不能自行增减接口。
- QA 在最后做交叉验证与真实运行验证，不通过则触发自动修复；开启 HITL 时由人做最终裁决。

### 2.3 关键技术选型与设计决策

**为什么放弃 SQLite，改用本地 JSON 文件存储？**

SQLite 系的 Node 驱动（`better-sqlite3` / `sqlite3`）大多是**原生模块**，`npm install` 时要走 `node-gyp` 现场编译，在很多机器上会因缺少编译工具链而失败，或拖慢安装，这直接破坏了"零配置、一键启动"的目标。

新版改为：后端只用 Node 内置 `fs` 读写一个 `data.json`，`db.js` 负责 `load()` / `save()`，文件不存在时返回按 `data_models` 生成的初始结构（每个集合一个空数组 + 一个自增计数器 `seq`）。代价是没有事务与并发控制，但对 hello-world MVP 完全够用，换来的是 `npm install` 只拉 `express` + `cors`，**秒过、零编译**。架构师 Prompt 里因此明令禁止任何数据库驱动。

**为什么前端 CSS 不经过 LLM，而由系统直接注入？**

让 LLM 在一个多文件 JSON 里把整张样式表当字符串值输出，转义极易出问题（换行、引号、URL 里的斜杠），CSS 经常被截断或损坏。系统把一套现代、克制、自带浅色/深色主题的 `BASE_CSS` 固定写在 `frontend_dev.py` 里，**直接注入，不经过 LLM**，因此永不丢失、永不出错。前端 Agent 只需产出 `index.html` 和 `app.js`，并使用约定好的类名。若模型仍附带了 CSS，则追加在注入主题之后，不覆盖。

**【v3.0 新增】为什么前端改用 Petite Vue，而不是继续手写原生 JS？**

原生 JS 方案让 LLM 自己写状态管理、事件委托、手动 DOM 重渲染，这是高频出错点：列表渲染常出现 `[object Object]`、事件绑不上、增删改后忘了刷新、手动拼 HTML 时还容易引入 XSS。v3.0 改用 **Petite Vue**——一个 6KB 的极简库，从 CDN 以 IIFE 形式加载（`window.PetiteVue.createApp(...).mount('#app')`），**无任何构建步骤**，浏览器直接打开即用。这样 LLM 只需做三件事：(1) 把数据与方法**平铺**在一个对象里作为根作用域；(2) 用 `v-model` / `v-for` / `@click` 等指令填 HTML 模板；(3) 在方法里调用 `fetch`。框架负责数据绑定与自动重渲染，把最容易错的部分接管掉。

> **一个值得说明的契约细节**：架构师产出的 `tech_stack.frontend` 仍写作 `vanilla-html-js`，而前端 Agent 实际用 Petite Vue 实现——二者并不冲突。因为前端 Agent 只消费 `api_contract` 与 `data_models`（见 3.3 的"契约即压缩边界"），**前端框架是它自身的实现细节**，在架构契约层面无需感知。换言之，换前端框架不影响前后端的合同，也不需要改架构师。

**为什么 QA 用确定性检查 + 真实运行，而不是再问一次 LLM？**

QA 是系统的"刹车"，目的是拦住 LLM 幻觉。如果再调一次 LLM 去判断"代码对不对"，既不可靠又要把全部代码塞进上下文（加剧上下文膨胀）。所以 QA 几乎全部用确定性手段：正则匹配、JSON 解析、`node --check`、以及真正把服务跑起来打接口。确定性检查对 token 的消耗为零，结论可复现。

**为什么仍然串行，而非并行 Backend 和 Frontend？**

真正让前后端"不打架"的是 **API Contract**——Frontend 在写代码时**并不阅读 Backend 生成的代码**，只阅读契约（见 3.3）。因此并行在正确性上其实是可行的。当前选择串行，主要是为了实现简单、执行确定、产物落盘顺序清晰，并保留"下游可参考上游文件清单"的余地。并行化留作 Future Work。

---

## 三、Agent 间通信机制

### 3.1 共享上下文 —— "黑板模式"（Blackboard Pattern）

Agent 之间**不直接通信**，而是通过一个共享的 `SharedContext` 对象读写数据：

```
SharedContext（黑板）
├── requirement      ← 用户原始需求
├── prd              ← PM 写入，Architect/Backend/Frontend 读取
├── architecture     ← Architect 写入，Backend/Frontend/QA 读取
│   └── api_contract ← 核心！前后端的"单一事实源"
├── backend_files    ← Backend 写入，DevOps/QA 读取
├── frontend_files   ← Frontend 写入（styles.css 由系统注入后并入），DevOps/QA 读取
├── devops_files     ← DevOps 写入
├── qa_report        ← QA 写入
├── qa_failures      ← 上一轮 QA 未通过的 check 列表，修复轮专用（用完即清空）
├── user_feedback    ← 【v3.0 新增】HITL 节点收集到的用户反馈 {agent_name: feedback_text}
└── history          ← 所有 Agent 的活动日志
```

### 3.2 为什么用黑板模式而非消息传递？

- **解耦**：Agent 之间无直接依赖，新增 Agent 只需读写黑板上的字段。
- **可审计**：每次写入都落盘到 `_agent_artifacts/` 目录，可回溯每个 Agent 的输入输出。
- **可断点续跑**：理论上可从中间某个 Agent 重新开始（中间产物都在磁盘上）。
- **避免互相打架**：每个 Agent 只写自己的字段、读别人的字段，不会互相覆盖。

### 3.3 上下文裁剪 —— `summary_for()` 与"契约即压缩边界"

这是本系统**特别值得强调**的机制，也是把整套系统压进一个小上下文窗口（见 5.2）的关键。

`summary_for(agent_name)` 并不把黑板上的全部内容丢给每个 Agent，而是只暴露下游需要的字段。它实际注入的内容按优先级为：

1. **用户原始需求** —— 所有 Agent 可见。
2. **PRD** —— 架构师及以后可见。
3. **技术架构（含 `api_contract`）** —— 后端 / 前端 / DevOps / QA 可见。
4. **用户反馈（HITL）** —— 【v3.0 新增】仅注入给当前被重新生成的 Agent（键名与 `Agent.name` 严格对应，如 `ProductManager` / `Architect`），其他 Agent 不会误读。
5. **QA 失败项清单** —— 仅在修复轮次中注入。

但**从不把上游生成的代码本身塞进 prompt**。这一点的精妙之处在于：**API Contract 充当了一道压缩边界。** 与其把 Backend 生成的几百行 `server.js` 喂给 Frontend，不如只喂那份契约——契约就是"Backend 对外行为"的压缩表示。这样每个 Agent 的 prompt 体量大致是 `|PRD| + |architecture|` 的量级，**与上游已经生成了多少代码无关**。这是控制上下文膨胀的核心抓手。

好处：

- **token 可控**：无论生成多少代码，下游 prompt 不随之线性膨胀。
- **避免无关信息干扰**：模型只看接口，不被实现细节带偏。
- **遵循最小权限原则**：每个 Agent 只看自己该看的。

### 3.4 API Contract —— 前后端的"合同"

这是整个系统中最关键的协调机制。Architect 产出的 `api_contract` 定义了每个接口的 `method`、`path`、请求体和响应体（如有筛选需求还会带 `query_params`）。Backend 和 Frontend 都必须严格遵守：

- **Backend**：只能实现 Contract 中声明的接口，不能增也不能减。
- **Frontend**：只能调用 Contract 中声明的接口，不能调用未声明的；同时**必须把每个接口都用上**（见 4.1 覆盖率检查）。

QA 会在最后双向验证这份一致性，形成闭环。

---

## 四、质量保障机制

### 4.1 QA Agent 的十一项确定性检查

QA 大量使用确定性检查（正则、JSON 解析、文件存在性、子进程），而非再调一次 LLM：

| # | 检查项 | 检查方式 | 目的 |
|---|--------|----------|------|
| 1 | 架构声明的文件全部生成 | 文件列表比对（归一化 `public/` 前缀） | 防漏 |
| 2 | `package.json` 合法且有 start 脚本 | JSON 解析 | 确保可启动 |
| 3 | 后端实现了 Contract 全部接口 | 正则匹配 `app.get/post/...`（路径归一化） | 防 Backend 漏接口 |
| 4 | 前端只调用 Contract 中的接口 | 正则提取 `fetch()` + **路径归一化**比对 | 防越界调用 |
| 5 | 前端实现了 Contract 的每一种操作 | 按 HTTP 方法反向核对覆盖率 | 防 Frontend 漏做删/改/标记完成 |
| 6 | `public/index.html` 以合法 `<!DOCTYPE html>` 开头 | 字符串前缀检查 | 拦 `<!--DOCTYPE`（漏感叹号）整页解析异常 |
| 7 | 后端用 `express.static` 托管前端 | 正则匹配 | 防访问 `/` 时 `Cannot GET /` |
| 8 | JS 语法检查 | `node --check`（utf-8 解码） | 防语法错误 |
| 9 | **运行时冒烟测试** | 真 `npm install` + 启动 + 打接口 | 拦"语法对但跑不起来" |
| 10 | 工程化文件齐全 | 文件存在性（`README.md` + `start.sh`） | 确保可交付 |
| 11 | **前端框架用法正确（Petite Vue）** | **静态特征检查** | **【v3.0 新增】拦"语法对、后端活，但前端框架用错、页面是死的"** |

> 关于第 10 项的微调：`.gitignore` 已不再作为 QA 的硬门槛（DevOps 仍会生成它），硬性要求收敛为 `README.md` 与 `start.sh` 两个最关键的交付文件。

### 4.2 运行时冒烟测试 —— 唯一能拦"语法对但跑不起来"的手段

`node --check` 只能查语法，查不出"语法没错但一启动就崩 / 一访问就 404"。第 9 项检查真正把项目跑起来：

1. `npm install`（超时 300s 即判失败）；
2. 选一个空闲随机端口，`node server.js` 启动服务；
3. 在 12s 内轮询就绪；若进程提前退出，判定"启动即崩溃"并回传 stderr 末几行；
4. `GET /` —— 验证静态托管（拿不到 HTML 多半是缺 `express.static`）；
5. `GET <契约里第一个集合级接口>` —— 验证读链路；
6. `POST <契约里第一个 POST>` —— 验证写链路，请求体从契约示例或 `data_models` 合成。

**通用性**：探测哪些接口、用什么请求体，全部从 `api_contract` / `data_models` 推断，不写死任何业务路径，因此对任意项目都适用。**容错口径**：4xx（校验未过 / 资源不存在）视为正常应用行为，只有启动崩溃、缺静态、5xx 才判失败。本机若无 node/npm，则跳过该项（判通过）。

> **重要边界（也是 4.4 第 11 项检查存在的原因）**：冒烟测试只启动**后端**、验证后端启动 + 静态托管 + 读写链路，它**不会在浏览器里执行前端 `app.js`**。因此前端若把框架 API 用错（页面其实渲染不出来），冒烟测试照样全绿——这块盲区由第 11 项静态检查补上。

### 4.3 自动修复循环 Fix Loop

编排器在 QA 之后进入最多 `MAX_QA_ROUNDS = 3` 轮的循环：

1. 把当前所有文件落盘（QA 的语法 / 冒烟检查依赖磁盘文件）。
2. 跑 QA；若 `all_passed` 则提前退出（开启 HITL 时改由人裁决，见 4.5）。
3. 否则用 `_get_failed_agents()` 把失败项**映射到具体 Agent**（`{Backend, Frontend, DevOps}` 的子集）。例如：缺 `server.js` / 契约接口未实现 / 缺 `express.static` / 冒烟启动失败 → Backend；缺前端操作 / `fetch` 越界 / `<!DOCTYPE>` 不对 / **Petite Vue 框架用法错误** → Frontend；缺工程化文件 → DevOps。
4. 把失败项写入 `ctx.qa_failures`，`summary_for()` 会**把这份失败清单作为 delta 注入**到被重跑 Agent 的 prompt，要求逐条修复。
5. 重跑相关 Agent 后**清空 `qa_failures`**，防止过期数据污染后续正常轮次。

只重跑"出问题的人"而非整条流水线，既省调用，也避免把已正确的产物推翻重来。

### 4.4 第 11 项检查：前端框架健全性（v3.0 新增）

这是为配合 Petite Vue 迁移新增的一道静态门禁。它解决的问题是：模型很容易"好心地"把 Petite Vue 当成标准 Vue3 来写——用 `data()` / `methods:` / `mounted()` 这套 Options API。在 Petite Vue 里这套**完全不被识别**，结果模板里的 `{{ }}`、`v-model` 全部失效、页面是死的；但 `node --check` 全过、冒烟测试（只跑后端）也全过，旧 QA 会误判全绿。

第 11 项用静态特征拦住这类回归，判定规则（任一不满足即失败）：

- **禁止出现 Vue3/Vue2 Options API 特征**：`data()`、`data:`、`computed:`、`watch:`、`new Vue` 等（Petite Vue 的 `createApp` 接收的是**扁平对象**，数据与方法平铺同级，没有这套结构）。
- **必须调用 `createApp(...)` 且存在 `.mount(...)`**，否则应用根本不会挂载。
- **必须有挂载后的初始化入口**：HTML 根元素带 `@vue:mounted="init()"`（或等价的 mount 后初始化调用），否则页面打开后不会自动拉取列表数据——这是 Petite Vue 最常见的 bug。

### 4.5 人工介入机制（Human-in-the-Loop）—— v3.0 全面升级

v2.0 的 HITL 只是"满意就继续 / 不满意就中止"两态：一旦不满意，只能推翻整条流水线重来。v3.0 把它升级成完整的**"审核 → 重生成"闭环**，并把所有交互逻辑集中到独立的 `human_review` 模块（编排器只负责调度）。通过 `--interactive` 或 `HUMAN_IN_LOOP=true` 开启。

每个审核节点返回一个统一的 `ReviewAction`，编排器据此决定走向。四种动作：

- **APPROVE（批准）**：通过，进入下一阶段。
- **EDIT（直接编辑）**：用户直接改产物（如 PRD 的功能列表），改后的内容写回黑板并通过。
- **FEEDBACK（反馈重生成）**：用户留下自然语言意见，存入 `ctx.user_feedback[agent]`，重跑该 Agent；`summary_for()` 会把这条反馈注入下一轮 prompt，要求逐条回应。重生成上限由 `MAX_REGEN = 3` 控制，达到上限则以最新版本继续。
- **ABORT（中止）**：直接终止整条流水线，避免在错误方向上浪费后续 Agent 调用。

三个审核节点：

| 节点 | 时机 | 支持的动作 |
|------|------|-----------|
| **PRD 审核** | PM 产出 PRD 后 | APPROVE / EDIT（改功能列表）/ FEEDBACK / ABORT |
| **架构审核** | Architect 产出技术方案后 | APPROVE / FEEDBACK / ABORT |
| **QA 审核** | 每轮 QA 跑完后 | **强制通过**（人工置 `all_passed=True`，报告摘要标注 `[人工强制通过]`，跳过剩余修复轮）/ **继续自动修复** / **中止** |

QA 审核把"机器全绿"与"人觉得够好"解耦：机器没通过但人认为可接受时可强制通过；机器通过但人想继续打磨时也能驱动更多轮修复。

### 4.6 LLM 输出可靠性保障

- **JSON Mode**：调用 LLM 时启用 `response_format: json_object`，强制 JSON 输出。
- **区分可重试与致命错误**：`JSONDecodeError`（格式错）→ 带错误反馈重试，最多 3 次让模型自我修正；其他 `Exception`（404/401/连接拒绝等 API 级错误）→ **立即抛出并附诊断信息**，不浪费重试次数（详见 5.10）。
- **代码围栏兜底**：部分模型即使在 JSON Mode 下仍会用 ```` ```json ```` 包裹，`_strip_code_fence()` 做兜底清理。
- **中间产物全落盘**：每个 Agent 的 prompt 和 output 都存到 `_agent_artifacts/`，便于人工审计和问题定位。

---

## 五、实现思路历程与踩坑记录

这一章记录从想法到能跑的真实推演路径，以及一路踩到的坑和对应的工程取舍——很多设计决策是被现实"逼"出来的。

### 5.1 起点：从"单 Agent 一把梭"到"流水线分工"

直接让一个模型"读需求 → 一次性吐出整个项目"。问题很明显：单次输出太长、模型容易在中途"忘记"前面的约束（接口对不上、前后端字段不一致、文件互相矛盾），而且产物一旦出错很难定位是哪个环节的锅。于是采用**职责分明的流水线**：PM 只澄清需求、Architect 只定契约、Backend/Frontend 各写一端、DevOps 收尾、QA 把关。每个环节产物更短、更聚焦，也天然可审计、可断点。

### 5.2 本地小模型的硬约束：显卡不足 → 把上下文窗口压到 8k

开发主要在本地用 Ollama 跑 `qwen-coder` 7B 级别的模型。第一道现实墙是**显存**：把上下文窗口开到 16k 时，KV cache 撑得很大、解码变慢，单次请求经常**超时**（`LLM_TIMEOUT` 已放到 600s 仍不稳）。把窗口降到 **8k** 后才能稳定跑完——`LLM_MODEL` 默认就叫 `qwen-coder-8k`，正是这个权衡的产物。

但 8k 窗口意味着**输入 prompt + 输出补全必须共享这点预算**，几乎没有冗余空间。这条硬约束直接决定了后面一连串"省 token"的设计——也就是 5.3。

### 5.3 上下文膨胀治理：把每个 Agent 的 prompt 摁在窗口里

流水线最容易犯的错是"把累积的全部上下文都丢给下一个 Agent"——到 Frontend/QA 时，上下文 = 需求 + PRD + 架构 + 后端全部代码 + 前端全部代码……轻松爆掉 8k。主要用了五条互补策略把它摁住：

1. **契约即压缩边界（最关键）**：下游 Agent 永远只拿到结构化的**契约**（PRD + architecture），**从不拿上游生成的代码**。契约是"上游对外行为"的浓缩，体量恒定，与生成了多少代码无关（见 3.3）。
2. **CSS 移出 LLM**：把整张样式表（约 2KB）从输入和输出里彻底拿掉，由系统注入。模型既不用读它也不用写它。
3. **确定性 QA，不再过 LLM**：QA 用正则/JSON/子进程判断，对上下文的占用为零；否则"让 LLM 审代码"就得把全部代码塞回窗口。
4. **修复轮只注入 delta**：修复时不重发全部上下文，只把**失败项清单**追加进 prompt，且用完即清空。
5. **极简技术栈顺带也压缩了 prompt**：JSON 文件存储没有建表 DDL、没有迁移、没有 ORM 模板要描述，数据模型只是几行字段列表，prompt 自然更短。

### 5.4 LLM 多文件 JSON 输出的"转义地狱"

让模型在一个 JSON 里把多份文件当字符串值输出，转义是重灾区：CSS/HTML/JS 里的换行、引号、正则字面量的斜杠都会破坏 JSON。对策有两层：

- **能不让模型写的就别让它写**：CSS 直接注入（5.3 第 2 条）。
- **必须让它写的就约束写法**：前端 Prompt 明确要求——HTML 一律用**模板字符串（反引号）+ 单引号属性**，**绝不用单引号字符串跨行拼接**（会直接 `SyntaxError`），**不写正则字面量**（要正则就 `new RegExp("...")`），只用 ASCII 引号。这些都是为了让生成的代码对 JSON 转义友好、对 `node --check` 友好。

### 5.5 放弃 SQLite：原生编译之痛 → 纯 `fs` JSON

初稿的 SQLite 方案在"一键启动"上栽了跟头：`better-sqlite3` 之类要现场编译原生模块，换台机器、缺工具链就 `npm install` 失败或巨慢。彻底改为 Node 内置 `fs` 读写 `data.json`：`db.js` 负责 `load/save`，文件不存在时按 `data_models` 返回初始结构并维护自增 `seq`。代价是无事务/并发，但 MVP 够用，换来"零编译、秒装"。架构师与后端 Prompt 都把数据库驱动列入黑名单，从源头杜绝模型"好心"引入。

### 5.6 QA 自己的 bug 反而逼坏了产物：路径归一化

这是最有意思的一个坑。早期 QA 的"前端越界调用"检查，只把**契约里**的 `:id` 归一化成 `:param`，却没处理契约实际使用的 **`{id}`**（OpenAPI 写法）。结果所有带参接口（删除、更新、标记完成）都被误判为"契约里没有、属于越界"。模型为了通过 QA，**干脆把删/改/标记完成的 `fetch` 全删掉**——于是"功能缺失"成了系统性现象，而且看起来还全绿。

教训：**检查器本身的 bug 会通过"为过检而妥协"反向污染产物。** 修法是让**前端与契约走同一个 `_norm_path`**，统一处理 `${id}` / `{id}` / `:id` 三种写法，比对口径一致后误判消失。

### 5.7 "全绿但功能缺失"：补一个覆盖率检查

5.6 修好越界误判后，又发现新问题：QA 只检查"有没有越界调用"，却不检查"有没有漏做操作"。于是 `app.js` 只做增/查、丢掉删/改也能全绿。新增 `_check_frontend_coverage`：按 HTTP 方法粒度**反向核对**——契约里出现的每种方法（GET/POST/PUT/DELETE/PATCH），前端代码里都得有对应的调用，否则判定"对应功能没做"。"不越界"和"不漏做"这才凑成双向约束。

### 5.8 "语法对但跑不起来"：补运行时冒烟 + DOCTYPE + 静态托管

`node --check` 全过，项目却可能：启动即崩溃；或缺 `express.static`，访问 `/` 直接 `Cannot GET /`；或 `index.html` 第一行写成 `<!--DOCTYPE`（漏感叹号）被当注释、整页解析异常。这些都是"语法层看不出、一跑才现形"的问题。对策是三个检查叠加：**运行时冒烟测试**（4.2，真跑真打接口）、**`<!DOCTYPE html>` 前缀检查**、**`express.static` 正则检查**。冒烟测试是其中的治本兜底——它是唯一能模拟真实用户访问、把"能不能跑起来"变成可判定信号的手段。

### 5.9 跨平台细节：Windows 解码崩溃、`npm.cmd`、随机端口

- **Windows GBK 解码崩溃**：早期对 `node --check` 的子进程用了 `text=True`，它按系统 locale（Windows 上是 GBK）解码 node 的 UTF-8 输出，遇到非 ASCII 直接抛 `UnicodeDecodeError`，导致 `stderr=None` 后续崩溃。改为显式 `encoding='utf-8', errors='replace'` 并对 `stderr/stdout` 做 `None` 防御。
- **`npm` 在 Windows 上是 `npm.cmd`**：`runner.py` 与 QA 冒烟测试统一在 Windows 上用 `shell=True` 调用固定字面量命令（`cwd` 为受信任的项目目录），三平台通吃。
- **冒烟测试用随机空闲端口**：先 `bind(("127.0.0.1", 0))` 让 OS 分配端口再释放，避免与本机已占用端口冲突。

### 5.10 区分"可重试"与"致命"错误，少走 3 次弯路

`chat_json` 的重试一开始是无脑重试。但 404（`LLM_BASE_URL` 没加 `/v1`）、401（key 无效）、连接拒绝（服务没起）这类是**配置/环境错误**，重试 3 次只是把同一个错误重复 3 遍、白等。改为：只对 `JSONDecodeError` 带着错误反馈重试（让模型自我修正格式），其余 `Exception` 立即抛出，并附上当前 `base_url` / `model` 和常见修复建议（如 Ollama 要用 `http://localhost:11434/v1`）。把"该重试的"和"该报错的"分开，定位问题快得多。

### 5.11 【v3.0】原生 JS 的"状态管理地狱" → 引入 Petite Vue

让 LLM 手写原生 JS 的状态管理，几乎是 bug 的温床：手动维护一份内存数据、手动监听事件、增删改之后手动重渲染列表，任何一处疏漏就出问题——最典型的是列表渲染成 `[object Object]`、按钮事件绑不上、写操作后忘了刷新、手拼 HTML 时埋下 XSS。这些都不是"语法错"，`node --check` 抓不到。

权衡之后改用 **Petite Vue**（6KB、CDN/IIFE、零构建）：把数据绑定与自动重渲染交给框架，LLM 只负责"声明数据 + 写模板 + 在方法里 `fetch`"。`{{ }}` 自动转义顺带解决了 XSS，`v-for` 自动跟随数据变化解决了"忘了刷新"。这是一次"用更强的约束换更稳的产物"的典型取舍——把容易错的环节从"靠模型自觉"变成"靠框架兜底"。

### 5.12 【v3.0】最隐蔽的坑：Petite Vue ≠ Vue3，"后端活、前端死"成新盲区

迁移到 Petite Vue 后立刻撞上一个极隐蔽的坑：模型见到 `createApp`，就**惯性地按标准 Vue3 的 Options API 来写**——`data() { return {...} }`、`methods: {...}`、`mounted() {...}`。但 Petite Vue 的 `createApp` 接收的是**扁平对象**（数据和方法平铺同级），根本不认识这套结构；它的生命周期也不会自动调用名为 `mounted` 的方法，必须靠 HTML 上的 `@vue:mounted="init()"` 触发。写错的后果是：模板里的 `{{ }}` 和 `v-model` 全部失效，**页面完全是死的**。

更要命的是，这个错误**所有既有检查都抓不到**：`node --check` 看语法没问题；运行时冒烟测试只启动后端、只看 `GET /` 是否返回 HTML（HTML 是注入的静态文件，照样返回），**它从不在浏览器里执行 `app.js`**。于是"后端完全正常、前端完全是死的"成了一个全新的盲区——这正是 5.6 / 5.8 同款教训的第三次重演：**某一层检查覆盖不到的地方，模型就会在那里出问题。**

对策两条：(1) 前端 Prompt 把 Petite Vue 的正确写法钉死——`const { createApp } = window.PetiteVue`、扁平对象、`.mount('#app')`、`@vue:mounted` 触发初始化，并明确列出"绝对禁止 `data()`/`methods:`/`mounted()`"；(2) 新增第 11 项 QA 静态检查（见 4.4），用正则特征拦住 Options API 写法、强制要求 `createApp` + `.mount(` + 挂载后初始化入口。至此"前端能不能渲染"也有了一个（尽力而为的）可判定信号。

### 5.13 【v3.0】HITL 从"开关"长成"闭环"

最初的人工介入只是一个**开关**：在 PRD / 架构后暂停，"满意就继续、不满意就中止"。问题是——不满意时除了推翻重来别无选择，用户那句"哪里不对、想怎么改"完全没被系统利用。

v3.0 把它长成**闭环**：

- PRD 节点支持**直接编辑**（用户改完功能列表写回黑板）或**反馈重生成**；
- 架构节点支持**反馈重生成**；
- 反馈通过新增的 `user_feedback` 字段存入黑板，`summary_for()` 在重生成轮次把它**定向注入**对应 Agent 的 prompt（键名与 `Agent.name` 严格对应，避免串号），要求逐条回应；
- 重生成有 `MAX_REGEN = 3` 的上限，达到则以最新版本继续，不会无限循环；
- QA 节点也加了**人工裁决**（强制通过 / 继续修复 / 中止），把"机器判定全绿"与"人觉得够好"解耦。

工程上还做了一次解耦重构：把所有交互逻辑收进独立的 `human_review` 模块，编排器只负责"在什么节点调用谁、拿到动作后怎么走"，调度与交互分离，后续要换成 Web 表单式审核也只需替换这一层。

---

## 六、代码结构与模块说明

```
fullstack-agent-team/
├── main.py                  # 主入口,CLI 参数解析(--interactive / --file / --output / --run)
├── orchestrator.py          # 流水线编排器 + QA 自动修复循环 + HITL 审核-重生成调度
├── human_review.py          # 【v3.0 新增】HITL 交互层:ReviewAction + review_prd/architecture/qa
├── runner.py                # QA 通过后一键 npm install + npm start + 开浏览器
├── config.py                # 全局配置(从 .env 读取,含 HUMAN_IN_LOOP / AUTO_RUN / RUN_PORT)
├── llm_client.py            # LLM 客户端(OpenAI 兼容,JSON 重试 + 围栏兜底 + 错误分级)
├── shared_context.py        # 共享上下文(黑板,含 qa_failures 与 user_feedback)
├── agents/                  # Agent 团队
│   ├── __init__.py          # 导出所有 Agent
│   ├── base_agent.py        # Agent 基类(日志、LLM 调用、prompt/产物落盘)
│   ├── product_manager.py   # 产品经理 Agent
│   ├── architect.py         # 架构师 Agent(契约 + 禁用数据库)
│   ├── backend_dev.py       # 后端工程师 Agent(Express + fs JSON)
│   ├── frontend_dev.py      # 前端工程师 Agent【v3.0 改用 Petite Vue,注入 BASE_CSS,仅产出 html/js】
│   ├── devops.py            # DevOps Agent
│   └── qa_engineer.py       # QA 测试 Agent【v3.0 共 11 项检查,含前端框架健全性】
├── api.py                   # 【工程内,本文档暂未覆盖,见 §7】浏览器 UI 的后端
├── web_ui.html              # 【工程内,本文档暂未覆盖,见 §7】浏览器 UI 页面
├── requirements.txt         # Python 依赖
├── .env.example             # 环境变量模板
└── output/                  # 生成的项目输出目录
    └── <project-name>/
        ├── server.js
        ├── db.js                 # fs 读写 data.json
        ├── package.json          # 仅 express + cors,无任何数据库依赖
        ├── data.json             # 运行后生成的本地数据(不入仓)
        ├── public/
        │   ├── index.html        # Petite Vue 模板(@vue:mounted 触发初始化)
        │   ├── app.js            # window.PetiteVue.createApp({...}).mount('#app')
        │   └── styles.css        # 由系统注入的固定主题
        ├── README.md
        ├── start.sh
        ├── .gitignore
        └── _agent_artifacts/     # 中间产物(prompt、输出、日志)
            ├── 01_pm_prompt.md
            ├── 01_pm_output.json
            ├── 02_architect_prompt.md
            ├── 02_architect_output.json
            ├── 03_backend_prompt.md
            ├── 03_backend_output.json
            ├── 04_frontend_v2_prompt.md
            ├── 04_frontend_v2_output.json
            ├── 05_devops_prompt.md
            ├── 05_devops_output.json
            ├── 06_qa_report.json
            └── agent_history.json
```

### 6.1 关键模块详解

**BaseAgent（基类）**：封装所有 Agent 共用逻辑——LLM 调用、prompt 落盘、产物落盘、日志。具体 Agent 只需实现 `system_prompt()` 和 `run()`，保持良好的开闭原则。

**LLMClient**：统一封装 OpenAI 兼容接口，支持 DeepSeek / 智谱 / Kimi / OpenAI / 本地 Ollama 等，通过 `.env` 切换、代码不改。核心是 `chat_json()`：JSON Mode + 失败重试（错误分级，见 5.10）+ 去代码围栏兜底。

**SharedContext**：黑板。集中管理中间产物、负责落盘、提供 `summary_for()` 上下文裁剪，并维护 `qa_failures`（修复轮 delta）与 `user_feedback`（HITL 反馈）的注入/清空。

**Orchestrator**：流水线编排器，系统的"指挥官"。决定 Agent 执行顺序、驱动 QA 自动修复循环（含失败项→Agent 的映射逻辑 `_get_failed_agents`），并在开启 HITL 时调度三个审核节点（调度与交互分离，交互细节交给 `human_review`），最终落盘、汇报、并按需自动启动。

**human_review（v3.0 新增）**：HITL 交互层，集中承载所有人工审核逻辑。对外暴露统一的 `ReviewAction`（APPROVE / EDIT / FEEDBACK / ABORT）与三个审核函数 `review_prd` / `review_architecture` / `review_qa`，分别在 PRD、架构、QA 三个节点向用户征询决策并返回结构化结果（动作、是否通过、编辑后的数据、反馈文本）。把交互从编排器里剥离出来，既让编排器保持纯粹的"调度"，也为将来替换成 Web 表单式审核留好了接口（见 §7）。

> 说明：`human_review.py` 的源码在本次整理中未能读取，以上为根据 `orchestrator.py` 对它的调用方式还原出的**行为契约**，接口与动作语义准确，内部实现细节以实际源码为准。

**Frontend Agent（v3.0 改写）**：质检前的"门面"。改用 Petite Vue（CDN/IIFE，零构建），只产出 `public/index.html` 与 `public/app.js`；`styles.css` 由系统注入的固定主题（`BASE_CSS`）提供，模型不再生成 CSS。Prompt 用大量篇幅钉死 Petite Vue 的正确写法（扁平对象、`@vue:mounted`、禁用 Options API），从源头降低 5.12 那类错误。

**QAAgent（v3.0 扩展）**：质量刹车。11 项确定性检查 + 运行时冒烟，详见第四章；其中第 11 项为本版新增的前端框架健全性静态检查。

**runner.py**：QA 通过且 `--run`/`AUTO_RUN=true` 时，自动 `npm install`，延迟 2.5s 开浏览器（给 server 留启动时间），再前台 `npm start`，阻塞至 Ctrl+C。无 node/npm 时优雅跳过并给出手动命令。

---

## 七、浏览器 UI（`api.py` + `web_ui.html`）—— 待补全

工程清单中包含 `api.py` 与 `web_ui.html` 两个文件，从命名看，这应是为系统提供的一套**浏览器图形界面**（让用户在网页里发起需求、查看流水线进度与产物，而不必走命令行）。

但本次整理时**这两个文件的内容未能读取到**，为避免臆测其具体实现（所用 Web 框架、接口设计、进度推送方式、是否复用 `orchestrator.run_pipeline`、HITL 在网页端如何呈现等），本文档暂不展开这一部分。

> 如果需要把浏览器 UI 也写进设计文档，请重新提供 `api.py` 与 `web_ui.html`（以及如有的 `human_review.py`、`config.py`），我可以据此补全本章，并把 §4.5 的 HITL、§6 的模块说明、流水线图等相应章节一并对齐到"Web 版"。

---

## 八、Future Work

v2.0 已落地的「Fix Loop（自动修复循环）」「端到端运行测试」之外，v3.0 又落地了「Petite Vue 前端迁移 + 框架健全性检查」「HITL 审核-重生成闭环」。后续可继续迭代的方向：

1. **Backend / Frontend 并行执行**：因为下游只依赖契约、不读上游代码（见 3.3），二者并行在正确性上已无障碍，可缩短总耗时。需要处理日志交错与产物写入顺序。
2. **更智能的修复定位**：当前失败项→Agent 是基于检查名的规则映射。可让冒烟测试回传更结构化的失败原因（哪个接口、什么 5xx），让修复 prompt 更精准，减少修复轮数。
3. **前端"能渲染"的运行时验证**：第 11 项目前是静态特征检查，仍是"尽力而为"。可引入无头浏览器（如 Playwright）真正加载页面、执行 `app.js`、断言列表渲染出来且增删改可用，把前端也纳入真实运行时冒烟。
4. **更大上下文 / 更强模型的弹性配置**：当前受限于本地小模型用了 8k 窗口（见 5.2）。可让 `summary_for()` 根据模型窗口大小自适应——窗口大时可附带更多上下文（如上游代码片段）以提质，窗口小则继续走极简策略。
5. **可选技术栈**：当前固定 Node + Express + JSON 文件 + Petite Vue。可扩展为用户可选（如需要并发/查询时切回真正的数据库，或换 Python/FastAPI 后端、换其它前端框架）。
6. **多轮增量迭代**：支持用户对已生成项目提修改意见，Agent 团队在现有产物上**增量更新**而非整体重生成——这与 v3.0 的 HITL 反馈机制天然契合。
7. **更丰富的冒烟覆盖**：当前只打"第一个集合级 GET + 第一个 POST"。可扩展为遍历契约全部接口、并对 PUT/DELETE/PATCH 做"先建后改/删"的链式断言。
8. **浏览器 UI 的体验增强**（待 §7 文件补全后明确）：实时进度推送、历史项目管理、网页端的 HITL 审核交互等。
